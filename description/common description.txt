как всё это выглядит?
запускаем прогон для create_tables
добываем объект таблица (getTable(String name))
для добытой таблицы забиваем данные (table.insert(String column, String value)) {либо класс row сделать}
запускаем sut, там отрабатывает магия из моего resultSet.
дальше проверяем, что вернулся тот объект, который ожидался.

! сделать DB initializer {dummy, X}
! сделать свой resultSet, который будет возвращаться при использовании jdbcTemplate

соответствует ли паттерну на создание таблицы?
Если да, то разбирать таблицу

<разбирать таблицу>
искать все определения колонок
сложить в Map: {{col1, number}, {col2, varchar}}
каждый map запихнуть в другой map: {table_name,{{col1, number}, {col2, varchar}}}


разобрать select выражение (select dummy from dual)
добыть имена таблиц и все колонки
списки: {dual} и {dummy}
для каждой таблицы убедиться, что есть такая колонка
имея на руках данные о содержимом таблиц, превратить их в нужный объект


какие задачи решаются?
проверка синтаксиса скриптов
проверка правильности написания кастов в объект
проверка того, что селекты вернут то, что нужно
покрытие кода unit-тестами увеличивается